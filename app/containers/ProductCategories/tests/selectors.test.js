// import { selectProductCategoriesDomain } from '../selectors';

describe('selectProductCategoriesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
