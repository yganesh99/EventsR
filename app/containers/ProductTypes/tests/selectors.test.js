// import { selectProductTypesDomain } from '../selectors';

describe('selectProductTypesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
